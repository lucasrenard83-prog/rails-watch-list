<div class="container mt-4">
  <h1> Watchlists </h1>
  <%= link_to "Créer une nouvelle liste", new_list_path, class: "btn btn-success mb-3" %>
  <div class="row">
    <% @lists.each do |list| %>
      <div class="col-12 col-md-6 col-lg-4 mb-3">
        <div class="card p-3 shadow-sm">
          <h2><%= link_to list.name, list %></h2>
        </div>
      </div>
    <% end %>
  </div>
</div>
