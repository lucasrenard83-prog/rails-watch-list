<div class="container mt-5">
  <div class="card shadow-sm p-5 mx-auto" style="max-width: 600px;">
    <h1 class="fw-bold mb-4">Ajouter un signet</h1>
    <%= simple_form_for [@list, @bookmark] do |f| %>
      <%= f.input :comment, label: "Commentaire" %>
      <%= f.association :movie, label: "Film", prompt: "Choisir un film" %>
      <%= f.submit "Créer le signet", class: "btn btn-success w-100 mt-3" %>
    <% end %>
    <%= link_to @list, class: "btn btn-secondary w-100 mt-3" do %>
      <i class="fa-solid fa-arrow-left me-1"></i> Retour
    <% end %>
  </div>
</div>
